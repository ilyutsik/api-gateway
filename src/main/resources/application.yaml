spring:
  application:
    name: api-gateway
  profiles:
    active: local
server:
  port: 8088
jwt:
  secret: ${JWT_SECRET}

user:
  service:
    url: ${USER_SERVICE_URI}
auth:
  service:
    url: ${AUTH_SERVICE_URI}
order:
  service:
    url: ${ORDER_SERVICE_URI}